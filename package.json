{
  "name": "real-time-event-driven-notification-system",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "build": "pnpm -r build",
    "lint": "node scripts/lint.mjs . && pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "node --test tests/*.test.mjs && pnpm -r test",
    "test:integration": "node --conditions=development --import tsx --test --test-concurrency=1 tests/integration/*.test.mjs",
    "test:e2e": "playwright install chromium && docker compose up -d --wait && playwright test",
    "compose:up": "docker compose up -d --wait",
    "compose:down": "docker compose down -v"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@testcontainers/kafka": "^11.5.0",
    "@testcontainers/postgresql": "^11.5.0",
    "@types/node": "^24.0.0",
    "@types/pg": "^8.15.0",
    "@types/ws": "^8.18.1",
    "jose": "^6.2.3",
    "kafkajs": "^2.2.4",
    "pg": "^8.16.0",
    "testcontainers": "^12.0.4",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0",
    "ws": "^8.21.1"
  }
}
